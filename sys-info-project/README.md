## Git Workflow Practice

In this project, I practiced advanced Git operations:

* **Branching:** Created `feature-logging` branch to develop the script.
* **Merging:** Used `git merge` to integrate features into `main`.
* **History:** Explored `git log --oneline` to track changes.
* **Status:** Used `git status` and `git diff` to audit code before commits.
